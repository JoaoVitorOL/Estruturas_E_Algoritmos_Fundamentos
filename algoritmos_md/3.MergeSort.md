# 🧩 Merge Sort (Ordenação por Mesclagem) O(n log n) | (PT-BR)

<img width="2560" height="1440" alt="image" src="https://github.com/user-attachments/assets/6dc95a07-f9d2-472d-8e5c-a07ea1973d1c" />

### 1. O problema da ordenação
Ordenar significa **reorganizar uma coleção de dados segundo um critério**, normalmente ordem crescente ou decrescente.

Exemplos clássicos:
- números em ordem crescente
- nomes em ordem alfabética
- registros por chave (ID, CEP, preço)

Antes de pesquisar dados de forma eficiente, ordenar é um passo fundamental.

---

## 🧩 Ideia central do Merge Sort

O Merge Sort é um algoritmo baseado na estratégia **Dividir e Conquistar**.
A ideia central é simples: :

> Divida o vetor em partes menores.
> Ordene cada parte separadamente.
> Combine (merge) as partes ordenadas em um único vetor ordenado.

Diferente do Bubble e do Insertion:
- Ele **não tenta ordenar tudo de uma vez**.
- Ele **quebra o problema até sobrar o menor caso possível**.
- Depois, **reconstrói tudo de forma ordenada**.

![GIF Animado](https://upload.wikimedia.org/wikipedia/commons/c/cc/Merge-sort-example-300px.gif)

---
# 👁️ Visualização do algoritmo

[Visualizar operação do algoritmo no Visualgo.net](https://visualgo.net/en/sorting)

---

````java
public class MergeSort {

    public static void mergesort(int[] array) {

        // Condição de parada da recusrão
        if (array.length <= 1) {
            return;
        }

        int divisao = array.length / 2;

        // esquerda <- cópia(array[0 até divisão])
        int[] esquerda = new int[divisao];
        System.arraycopy(array, 0, esquerda, 0, divisao);

        // direita <- cópia(array[divisão até final])
        int[] direita = new int[array.length - divisao];
        System.arraycopy(array, divisao, direita, 0, array.length - divisao);

        // Ordena recursivamente cada metade
        mergesort(esquerda);
        mergesort(direita);

        // Mescla as duas partes ordenadas
        merge(array, esquerda, direita);
    }
}


````

## O que cada parte do algoritmo faz ?

````java
if (array.length <= 1) {
    return;
}

````
- Caso base da recursão, ponto de parada.
- Tamanho do array é 1 ou menor? Não é mais divisível pela metadade, para a recursão.

 ````java
int divisao = array.length / 2;

````
- Define o ponto médio do vetor
- Garante que o array será quebrado aproximadamente ao meio
- Isso gera a profundidade log₂(n) da recursão
  
 ````java
int[] esquerda = new int[divisao];
System.arraycopy(array, 0, esquerda, 0, divisao);

int[] direita = new int[array.length - divisao];
System.arraycopy(array, divisao, direita, 0, array.length - divisao);

````
- Cria novos vetores independentes.
- Cada chamada recursiva trabalha com sua própria cópia.
> Cada nível da recursão tem seus próprios esquerda e direita

 ````java
mergesort(esquerda);
mergesort(direita);

````
- O que acontece conceitualmente:
 ````java
    [8, 3, 5, 4, 2, 6, 1, 7]
              |
[8, 3, 5, 4]     [2, 6, 1, 7]
     |                  |
[8,3] [5,4]      [2,6] [1,7]
   |     |          |     |
 [8][3] [5][4]    [2][6] [1][7]

````
- A divisão continua até restarem vetores unitários

### 🧩 O Merge (a parte mais importante)
 ````java
private static void merge(int[] array, int[] esquerda, int[] direita) {

    int indiceEsquerda = 0;
    int indiceDireita = 0;
    int indiceFinal = 0;

    while (indiceEsquerda < esquerda.length && indiceDireita < direita.length) {

        if (esquerda[indiceEsquerda] < direita[indiceDireita]) {
            array[indiceFinal] = esquerda[indiceEsquerda];
            indiceEsquerda++;
        } else {
            array[indiceFinal] = direita[indiceDireita];
            indiceDireita++;
        }
        indiceFinal++;
    }

    while (indiceEsquerda < esquerda.length) {
        array[indiceFinal] = esquerda[indiceEsquerda];
        indiceEsquerda++;
        indiceFinal++;
    }

    while (indiceDireita < direita.length) {
        array[indiceFinal] = direita[indiceDireita];
        indiceDireita++;
        indiceFinal++;
    }
}
 
````
🔹Exemplo visual do começo ao fim

````text
esquerda = [3, 7, 9]
direita  = [2, 4, 8]
array    = [ ?, ?, ?, ?, ?, ? ]

int indiceesquerda = 0;
int indicedireita = 0;
int indicefinal = 0;


Estado inicial:
indiceesquerda = 0 → aponta para esquerda[0] = 3
indicedireita  = 0 → aponta para direita[0]  = 2
indicefinal    = 0 → posição onde escreveremos em array

visual:

esquerda: [3, 7, 9]
            ↑
direita : [2, 4, 8]
            ↑
array   : [ ?, ?, ?, ?, ?, ? ]
            ↑

LOOP PRINCIPAL (O CORAÇÃO DO MERGE)
Esse while só roda enquanto os dois lados ainda têm elementos disponíveis.

while (indiceesquerda < esquerda.length && indicedireita < direita.length)

ITERAÇÃO 1
Comparação:
if (esquerda[indiceesquerda] < direita[indicedireita])
Valores comparados:
3 < 2 ❌

Então cai no else:
array[indicefinal] = direita[indicedireita];
indicedireita++;
indicefinal++;

Estado após a escrita:
array[0] = 2

Índices atualizados:
indicedireita = 1
indicefinal   = 1

Visual agora:

esquerda: [3, 7, 9]
            ↑
direita : [2, 4, 8]
               ↑
array   : [ 2, ?, ?, ?, ?, ? ]
               ↑

ITERAÇÃO 2
Comparação:
3 < 4 ✅

executa o if:
array[indicefinal] = esquerda[indiceesquerda];
indiceesquerda++;
indicefinal++;

resultado:
array[1] = 3

índices:
indiceesquerda = 1
indicefinal    = 2

visual:
esquerda: [3, 7, 9]
               ↑
direita : [2, 4, 8]
               ↑
array   : [ 2, 3, ?, ?, ?, ? ]
                    ↑

ITERAÇÃO 3
Comparação:
7 < 4 ❌

executa o else:
array[2] = 4

índices:
indicedireita = 2
indicefinal   = 3


Visual:
esquerda: [3, 7, 9]
               ↑
direita : [2, 4, 8]
                 ↑
array   : [ 2, 3, 4, ?, ?, ? ]
                     ↑
ITERAÇÃO 4
Comparação:
7 < 8 ✅

executa o if:
array[3] = 7

índices:
indiceesquerda = 2
indicefinal    = 4

esquerda: [3, 7, 9]
                 ↑
direita : [2, 4, 8]
                 ↑
array   : [ 2, 3, 4, 7, ?, ? ]
                        ↑
ITERAÇÃO 5
Comparação:
9 < 8 ❌

executa o else:
array[4] = 8

índices:
indicedireita = 3
indicefinal   = 5

esquerda: [3, 7, 9]
                 ↑
direita : [2, 4, 8] (acabou)
array   : [ 2, 3, 4, 7, 8, ? ]
                           ↑
SAÍDA DO LOOP PRINCIPAL
O while para porque indicedireita == direita.length
O lado direito acabou.
Mas, o lado esquerdo ainda tem elementos.

LOOP FINAL — SOBRA DA ESQUERDA
while (indiceesquerda < esquerda.length)

Elemento final esquerda[2] = 9

array = [2, 3, 4, 7, 8, 9]


````


---

# Complexidade (Big-O)
## O que é o Big O?
Muitas pessoas confundem o Big O como um sinônimo de tempo. Mas, na verdade, o Big O mede **a quantidade de comparações|operações** que o algoritmo realiza conforme o tamanho da entrada n aumenta.
Ou seja, Big-O **não mede tempo em segundos**. Na realidade, **mede crescimento do número de operações**.
O fato de demorar menos tempo para realizar o trabalho é decorrente da redução do número de comparações necessárias para organizar elementos.
Big O é utilizado para visualizar como um algoritmo **escala**.


<img width="800" height="556" alt="image" src="https://github.com/user-attachments/assets/2ecca399-6b62-4a71-a0b3-b8f87c42f391" />

[Big O Cheat Sheet](https://www.bigocheatsheet.com/)

O Big O nos mostra a **tendência de crescimento**.
> Se eu multiplicar n por 10, o custo cresce:
- linearmente?
- quadraticamente?
- exponencialmente?

Ignora:

- velocidade do computador
- linguagem
- constantes pequenas

Observa apenas **como o algoritmo escala**.

Pergunta central:
> Se n (tamanho do array) dobra, o trabalho dobra, quadruplica ou explode?

---

# 🃏 Big O do Insertion Sort O(n²)

```text
para i de 1 até n-1:
    marcado = A[i]
    j = i - 1

    enquanto j >= 0 e A[j] > marcado:
        compara
        desloca A[j] para a direita
        j--
````
Isso já nos diz algo importante:
> O elemento marcado pode ser comparado com **todos os elementos anteriores**.

**Imagine n = 10**
````text
Volta 0 → 9 comparações
Volta 1 → 8 comparações
Volta 2 → 7 comparações
Volta 3 → 6 comparação
Volta 4 → 5 comparação
Volta 5 → 4 comparação
Volta 6 → 3 comparação
Volta 7 → 2 comparação
Volta 8 → 1 comparação

9 + 8 + 7 + 6 + 5 + 4 + 3 + 2 + 1 = 45
````
Agora generalizando para n elementos:
````text
(n - 1) + (n - 2) + ... + 1

(Representa o número total de comparações feitas pelo algoritmo)

Essa soma é conhecida matematicamente como:

n(n - 1) / 2

A divisão por 2 acontece porque o total de comparações no Bubble Sort segue uma sequência que diminui passo a passo (PA decrescente).
Se você somar essa sequência com ela mesma ao contrário, todos os pares de números darão o mesmo resultado, o que acaba gerando o dobro da soma que você precisa.
Por isso, dividimos por 2: para eliminar essa repetição e encontrar o número real de comparações.

````

Vamos expandir:
````text
n(n - 1) = n² - n

Dividindo por 2:

(n² - n) / 2

````
A parte importante disso tudo é que a fórmula (n² - n) / 2 tem n² como seu termo dominante ! <br>
Sendo assim, **o Big O do Insertion Sort cresce quadraticamente** O(n²) . <br>

---

# Insertion Sort — Melhor, Médio e Pior Caso


### 🔴 Pior caso — O(n²)
O pior caso ocorre quando o vetor está totalmente em ordem inversa.
```text
[5, 4, 3, 2, 1]
````
O que acontece
- Cada novo elemento precisa atravessar toda a parte ordenada


### 🟠 Caso médio — O(n²)
O vetor está parcialmente desordenado, com elementos fora de posição de forma aleatória.
```text
[3, 1, 2, 4, 5]
````
O que acontece

- Algumas comparações trocam
- Outras não
- Mas todas as comparações ainda acontecem

### 🟢 Melhor caso — O(n)
 O vetor já está totalmente ordenado.
 ```text
[1, 2, 3, 4, 5]
````
O que acontece:
- O while executa uma única comparação
- O break ocorre imediatamente
